// Create a separate file for post data
const postsDatabase = [
    // All your posts here
];

// Export for use in other scripts
if (typeof module !== 'undefined') {
    module.exports = { postsDatabase };
}
